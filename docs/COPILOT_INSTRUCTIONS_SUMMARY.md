# Copilot Instructions Summary

## Status
This file is archival. The legacy Copilot and rebuild bundles previously stored under `/docs/jamarq-copilot-docs` and `/docs/portfolio-instructions-v2` are no longer active implementation guidance.

## Current Guidance
- Use `docs/SYSTEM_OVERVIEW.md` as the system entry point.
- Use `docs/SYSTEM_PUBLIC.md` for public routes and content conventions.
- Use `docs/DEVELOPER_GUIDE.md` for local development and code conventions.
- Use `docs/DEPLOYMENT_GUIDE.md` and `docs/SYSTEM_OPS.md` for deployment and operations.

## Retired Assumptions
Do not reintroduce these legacy assumptions unless the project is explicitly reopened for that work:
- Placeholder/Phase 0 build flow
- `src/` directory layout
- Sanity CMS-backed portfolio content
- Framer Motion animation system
- Inter/Montserrat typography assumptions

## Permanent Guardrail
"Build with intention." must remain unchanged wherever it appears.
